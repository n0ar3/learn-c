#include <stdio.h>

int main() {
  /**
   *  arithmetic operators = + - * / % ++ --
   */

  int x = 10;
  int y = 3;
  int z = 0;

  // Float is needed for divide
  // float y = 3;
  // float z = 0;

  // z = x + y;
  // z = x - y;
  // z = x * y;
  // z = x / y;
  // z = x % y;
  // printf("%d\n", z);

  // x++;
  // x--;
  // x += 2;
  // x -= 3;
  // x *= 2;
  // x /= 2;
  // x %= 2;

  printf("%d\n", x);

  return 0;
}
